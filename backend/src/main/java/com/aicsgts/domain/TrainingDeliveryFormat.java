package com.aicsgts.domain;

/** How a training program is delivered (MVP taxonomy; maps to spec provider and format filter). */
public enum TrainingDeliveryFormat {
  ONLINE,
  IN_PERSON,
  HYBRID,
  CERTIFICATION
}
